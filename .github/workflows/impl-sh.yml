name: impl-sh
on:
  workflow_call:
    inputs:
      runner_label: {required: true, type: string}
permissions:
  contents: read
jobs:
  probe:
    # T2: [self-hosted, "<expr>"] array form, from workflow_call.
    # No such runner exists -> expect QUEUED (syntax accepted), not FAILURE.
    runs-on: [self-hosted, "${{ inputs.runner_label }}"]
    steps:
      - run: echo "unreachable - no runner carries this label"
